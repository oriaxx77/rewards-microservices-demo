server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/oriaxx77/rewards-microservices-demo
          searchPaths: server-config
          
  
